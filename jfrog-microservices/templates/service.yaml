apiVersion: v1
kind: Service
metadata:
  name: {{ include "jfrog-microservices.fullname" . }}
  labels:
    {{- include "jfrog-microservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jfrog-microservices.selectorLabels" . | nindent 4 }}
